spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/apiprodutostest?createDatabaseIfNotExist=true
    username: root
    password: admin
  flyway:
    enabled: false
